<h2>ジャンル編集</h2>
<%= form_with model: @genre,url:admin_genre_path do |f| %>
<span>ジャンル名</span>
<span><%= f.text_field :name, class: "form-control" %></span>
<span><%= f.submit "変更を保存", class: "btn btn-secondary" %></span>
<% end %>
